#include <iostream>
#include <string>
#include <vector>
#include <ctime>
#include <cstdlib>

using namespace std;

void tampilkanMenu();
void login();
void mainkanLudo();
void tampilkanPapan();
void daftarPengguna();
void lemparDadu();
int pemainSaatIni = 0; 

struct Pengguna {
    string namaPengguna;
    string kataSandi;
    int posisi; 
};

vector<Pengguna> pengguna;


int main() {
    srand(time(0)); 
    tampilkanMenu();
    return 0;
}

void tampilkanMenu() {
    int pilihan;
    do {
        cout << "===== Menu Game Ludo =====" << endl;
        cout << "1. Login" << endl;
        cout << "2. Daftar" << endl;
        cout << "3. Mainkan Ludo" << endl;
        cout << "4. Keluar" << endl;
        cout << "Masukkan pilihan Anda: ";
        cin >> pilihan;
